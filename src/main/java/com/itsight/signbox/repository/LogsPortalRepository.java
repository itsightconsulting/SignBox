package com.itsight.signbox.repository;

import com.itsight.signbox.domain.LogsPortal;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LogsPortalRepository extends JpaRepository<LogsPortal, Integer> {
}
